{
    "name": "padnis",
    "version": "1.0.0",
    "dependencies": {
      "bcrypt": "^5.1.0",
      "jsonwebtoken": "^9.0.0",
      "mongoose": "^6.10.0"
    },
    "scripts": {
      "vercel-build": "cd frontend && npm install && npm run build"
    }
  }